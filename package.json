{
  "name": "tampermonkey-private-shield",
  "version": "1.4.0",
  "description": "Tampermonkey userscript that auto-hides emails, phone numbers, and IP addresses during screensharing",
  "private": true,
  "scripts": {
    "test": "echo \"No tests configured yet\" && exit 0",
    "lint": "eslint private-shield.user.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
